#!/bin/bash

if ! { ([ "$TERM" = "screen-256color" ] || [ "$TERM" = "screen" ]) && [ -n "$TMUX" ]; } then
    if [ "$*" == "" ]; then
        tmux attach -t 0 || tmux new -s 0
    else
        tmux attach -t $1 || tmux new -s $1
    fi
fi

